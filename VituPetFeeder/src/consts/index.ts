import { buttonFontSize, windowPercentage } from './sizes';
import {
  DEVICE_AP_SSID,
  DEVICE_AP_PASSWORD,
  setupNewDeviceInstructionMessage,
  buttonTitle,
  modalTitle,
} from './strings';

export {
  windowPercentage,
  buttonFontSize,
  DEVICE_AP_SSID,
  DEVICE_AP_PASSWORD,
  setupNewDeviceInstructionMessage,
  buttonTitle,
  modalTitle,
};
