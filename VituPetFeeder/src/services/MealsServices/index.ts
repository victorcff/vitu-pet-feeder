import {
  activateMeal,
  createMeal,
  deleteMeal,
  getAllMealsFromDevice,
  updateMeal,
} from './api';

const MealsServices = {
  createMeal,
  updateMeal,
  getAllMealsFromDevice,
  deleteMeal,
  activateMeal,
};

export default MealsServices;
