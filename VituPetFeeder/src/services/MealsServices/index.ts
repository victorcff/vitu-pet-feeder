import {
  createMeal,
  deleteMeal,
  getAllMealsFromDevice,
  updateMeal,
} from './api';

const MealsServices = {
  createMeal,
  updateMeal,
  getAllMealsFromDevice,
  deleteMeal,
};

export default MealsServices;
