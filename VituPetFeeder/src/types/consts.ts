type WindowPercentage = {
  small: number;
  medium: number;
  large: number;
};

type ButtonFontSize = {
  small: number;
  medium: number;
  large: number;
};

type Paddings = {
  screenBorder: number;
};

export type { WindowPercentage, ButtonFontSize, Paddings };
